<layout xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <!-- Variable to bind the movie object -->
        <variable
            name="movie"
            type="com.example.moviedb.database.Movie" />
    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        app:cardBackgroundColor="@color/cardBackground"
        app:cardCornerRadius="8dp"
        app:cardElevation="4dp"
        android:layout_marginBottom="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- Movie Name -->
            <TextView
                android:id="@+id/movieNameText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="Movie Name"
                android:text="@{movie.name}" /> <!-- Bind the movie's name -->

            <!-- Movie Theatre -->
            <TextView
                android:id="@+id/movieTheatreText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@{movie.theatre}" /> <!-- Bind the movie's theatre -->

            <!-- Movie Cost -->
            <TextView
                android:id="@+id/movieCostText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="@{`Cost: $${movie.cost.toString()}`}" /> <!-- Format and bind movie cost -->

        </LinearLayout>
    </androidx.cardview.widget.CardView>
</layout>
